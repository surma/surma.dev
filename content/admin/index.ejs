---
layout: ""
---
<meta name="viewport" content="minimum-scale=1, width=device-width" />
<style>
  label {
    margin: 10px 0;
  }
  form {
    display: flex;
    flex-direction: column;
  }
</style>
<span></span>
<datalist id="location-list">
  <%-
    [...new Set(collections.photo.map(x => x.data.location))]
      .map(l => `<option value="${l}"></option>`)
      .join("")
  %>
</datalist>
<script src="emitChunk(/admin.js)"></script>